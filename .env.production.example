# pPonto — PRODUCTION environment variables
# Copy to .env.production (NEVER commit real tokens).
# Used for production builds / CI deploys.

# Turso PRODUCTION database (schema-identical to dev `pponto`)
VITE_TURSO_URL=libsql://pponto-prod-felipeumbra.aws-ap-northeast-1.turso.io
VITE_TURSO_TOKEN=your-turso-prod-token-here