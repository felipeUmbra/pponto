/**
 * Ajuste placeholder — Punch adjustment request.
 */
export function renderAjuste(): HTMLElement {
  const el = document.createElement('div');
  el.innerHTML = '<p class="text-caption text-outline text-center py-8">Solicitação de Ajuste — Fase 2</p>';
  return el;
}
