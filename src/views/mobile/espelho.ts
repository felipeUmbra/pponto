/**
 * Espelho de ponto placeholder — Time mirror.
 */
export function renderEspelho(): HTMLElement {
  const el = document.createElement('div');
  el.innerHTML = '<p class="text-caption text-outline text-center py-8">Espelho de Ponto — Fase 2</p>';
  return el;
}
