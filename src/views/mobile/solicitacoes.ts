/**
 * Solicitações placeholder — Leave requests & atestados.
 */
export function renderSolicitacoes(): HTMLElement {
  const el = document.createElement('div');
  el.innerHTML = '<p class="text-caption text-outline text-center py-8">Solicitações e Atestados — Fase 2</p>';
  return el;
}
